Module Variables
    Sub Main()
        ' Variables are decalred using `Dim <var name> As <type>`
        Dim myNum As Integer
        myNum = 5
        Dim nam As String
        nam = "Asim"

        Dim x As Integer
        Dim y As Integer
        x = 32
        y = 15

        Console.WriteLine(nam)
        Console.WriteLine(myNum)
        Console.WriteLine(x+y)
    End Sub
End Module
